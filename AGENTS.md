# 项目工程规则

以下规则用于本仓库中的 TypeScript 和 JavaScript 工作；更具体目录中的 `AGENTS.md` 优先。

## 语言与注释

- 面向用户的沟通、代码注释和文档注释默认使用中文；不可翻译的协议字段和代码标识符保留原文。
- 每个新增或修改的函数、方法必须添加标准文档注释，说明用途、契约、副作用与重要约束。
- 新增或修改的类型、成员、常量和固定数据必须添加中文注释，说明用途、单位、范围和状态约束。
- 核心执行步骤应添加中文注释解释业务目的，不得逐行复述实现。

## 工程初始化

- 新对话先检查 `codegraph` 与 `.codegraph`；缺失时安装并执行 `codegraph init`。
- 检查并保留现有 Prettier、调试和 VS Code 配置，只补齐缺失字段，不重复依赖或覆盖项目设置。

## 格式化与调试

- Prettier 固定使用 `singleQuote: true`、`trailingComma: "all"`、`tabWidth: 4`、`printWidth: 120`、`semi: true`、`bracketSpacing: true`、`arrowParens: "avoid"`、`endOfLine: "lf"`、`embeddedLanguageFormatting: "auto"`、`quoteProps: "as-needed"`。
- 提供可运行的格式化和校验脚本，并忽略依赖、构建产物、日志和生成文件。
- VS Code 保存时执行格式化、ESLint 修复和导入整理；前端、服务端及组合调试入口必须可直接使用。

## 结构与维护

- 修改前识别语言、框架、构建系统和版本；保留现有结构与模式，代码放在最接近的项目标准位置。
- 源码、测试、配置、文档、静态资源和生成文件按既有约定分离，不创建推测性目录或抽象。
- 命名应清晰且符合领域；模块和函数保持单一职责，优先复用项目工具、标准库与已有依赖。
- 改动严格限定在当前需求；非平凡行为必须有针对性测试。
- 完成前运行相关格式化、代码检查、类型检查、构建和测试。
- 冲突时依次遵循用户明确要求、最近的项目 `AGENTS.md`、项目既有约定和通用建议，并简要记录权衡。
